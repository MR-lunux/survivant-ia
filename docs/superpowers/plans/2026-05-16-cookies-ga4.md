# Cookies + Google Analytics 4 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Mettre en place Google Analytics 4 (`G-KTGGEP09JY`) avec une gestion de consentement RGPD/LPD conforme, activer PostHog en mode complet sous consentement (cookies + session recordings + heatmaps), et mettre à jour la politique de confidentialité.

**Architecture:** Composable `useConsent` source unique de vérité (localStorage 13 mois). Deux plugins client (`gtag.client.ts` nouveau, `posthog.client.ts` patché) qui lisent le consentement à l'init et watchent ses changements. Composant `CookieBanner.vue` (slide-up bas + modal personnalisation) monté globalement dans `app.vue`. Nouvelle page `/cookies`, section "Légal" ajoutée à `AppFooter`, `/confidentialite` actualisée. Google Consent Mode v2 utilisé pour préserver l'attribution Google Ads en cas de refus ; PostHog garde son mode cookieless en fallback.

**Tech Stack:** Nuxt 4, Vue 3, TypeScript, posthog-js (déjà installé), gtag.js (chargé via script tag, pas de module). Pas de framework de tests dans le projet → vérification **manuelle** dans le navigateur à chaque tâche.

**Spec :** `docs/superpowers/specs/2026-05-16-cookies-ga4-design.md`

**Note importante :** Trois points découverts en explorant le code, à appliquer dans le plan plutôt que la spec :
- Le CSS variable utilisée est `--color-surface-2` (#1A1A14), pas `--color-bg-elevated` (qui n'existe pas).
- Le CSP actuel (`script-src 'self' 'unsafe-inline'`) **bloque** `googletagmanager.com` — la tâche 1 le corrige.
- L'AppFooter actuel n'a pas de section "Légal" ; on en crée une.

---

## Task 1 : Env vars, runtimeConfig, CSP

**Files:**
- Modify: `/Users/mathieu/Documents/survivor/.env.example`
- Modify: `/Users/mathieu/Documents/survivor/.env`
- Modify: `/Users/mathieu/Documents/survivor/nuxt.config.ts`

Ajoute la variable d'environnement `NUXT_PUBLIC_GTAG_ID`, l'expose via `runtimeConfig.public.gtagId`, et autorise les domaines Google dans le CSP.

- [ ] **Step 1: Ajouter `NUXT_PUBLIC_GTAG_ID` à `.env.example`**

Append à la fin du fichier :

```bash

# Google Analytics 4 — mesure ID public (préfixe G-...)
NUXT_PUBLIC_GTAG_ID=G-KTGGEP09JY
```

- [ ] **Step 2: Ajouter la même variable à `.env`**

Append :

```bash
NUXT_PUBLIC_GTAG_ID=G-KTGGEP09JY
```

- [ ] **Step 3: Exposer `gtagId` dans `runtimeConfig.public` (nuxt.config.ts)**

Édite `nuxt.config.ts` ligne 21-23. Remplace :

```ts
    public: {
      posthogKey: '',
    },
```

par :

```ts
    public: {
      posthogKey: '',
      gtagId: '',
    },
```

- [ ] **Step 4: Autoriser googletagmanager dans le CSP**

Édite `nuxt.config.ts` ligne 63. Remplace le header `Content-Security-Policy` :

```ts
        'Content-Security-Policy': "default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob: https:; font-src 'self' data:; media-src 'self' blob:; connect-src 'self'; frame-ancestors 'none'; form-action 'self'; base-uri 'self';",
```

par :

```ts
        'Content-Security-Policy': "default-src 'self'; script-src 'self' 'unsafe-inline' https://www.googletagmanager.com; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob: https:; font-src 'self' data:; media-src 'self' blob:; connect-src 'self' https://www.google-analytics.com https://*.google-analytics.com https://*.analytics.google.com https://www.googletagmanager.com; frame-ancestors 'none'; form-action 'self'; base-uri 'self';",
```

Diff exact : ajout de `https://www.googletagmanager.com` à `script-src`, et ajout de `https://www.google-analytics.com https://*.google-analytics.com https://*.analytics.google.com https://www.googletagmanager.com` à `connect-src`. PostHog passe par le reverse-proxy `/_ph/**` donc reste couvert par `'self'`.

- [ ] **Step 5: Vérifier en dev**

Lance :

```bash
npm run dev
```

Ouvre la console du navigateur sur `http://localhost:3000`. Tape :

```js
console.log(useRuntimeConfig().public.gtagId)
```

**Attendu :** `'G-KTGGEP09JY'`. Pas d'erreur CSP dans la console (rien chargé encore, mais le header doit être bien posé — vérifier dans Network > Headers > Content-Security-Policy).

- [ ] **Step 6: Commit**

```bash
git add .env.example nuxt.config.ts
git commit -m "feat(analytics): add GA4 env var, expose runtimeConfig, allow googletagmanager in CSP"
```

(Note : `.env` est gitignored, ne pas tenter de le committer.)

---

## Task 2 : Composable `useConsent`

**Files:**
- Create: `/Users/mathieu/Documents/survivor/app/composables/useConsent.ts`

Source unique de vérité pour l'état de consentement. Lit/écrit `localStorage['cookie-consent']`, gère l'expiration 13 mois, expose un state réactif et trois actions (`accept`, `refuse`, `openModal`).

- [ ] **Step 1: Écrire le composable**

Crée le fichier `app/composables/useConsent.ts` avec le contenu suivant :

```ts
// app/composables/useConsent.ts
// Source unique de vérité pour l'état de consentement cookies.
// Voir docs/superpowers/specs/2026-05-16-cookies-ga4-design.md §Flow de consentement.

const STORAGE_KEY = 'cookie-consent'
const CONSENT_VERSION = 1
const TTL_MS = 13 * 30 * 24 * 60 * 60 * 1000 // ~13 mois

type StoredConsent = {
  analytics: boolean
  timestamp: number
  version: number
}

export type ConsentStatus = 'pending' | 'granted' | 'denied'

type ConsentState = {
  analytics: ConsentStatus
  needsChoice: boolean
  isLoaded: boolean
}

function readStorage(): StoredConsent | null {
  if (typeof window === 'undefined') return null
  try {
    const raw = window.localStorage?.getItem(STORAGE_KEY)
    if (!raw) return null
    const parsed = JSON.parse(raw) as StoredConsent
    if (parsed.version !== CONSENT_VERSION) return null
    if (typeof parsed.timestamp !== 'number') return null
    if (Date.now() - parsed.timestamp > TTL_MS) return null
    return parsed
  } catch {
    return null
  }
}

function writeStorage(analytics: boolean) {
  if (typeof window === 'undefined') return
  try {
    window.localStorage?.setItem(STORAGE_KEY, JSON.stringify({
      analytics,
      timestamp: Date.now(),
      version: CONSENT_VERSION,
    } satisfies StoredConsent))
  } catch {
    // private browsing, quota, etc. : on tolère silencieusement.
  }
}

export function useConsent() {
  const state = useState<ConsentState>('cookie-consent', () => ({
    analytics: 'pending',
    needsChoice: true,
    isLoaded: false,
  }))

  // Init lazy côté client uniquement.
  if (typeof window !== 'undefined' && !state.value.isLoaded) {
    const stored = readStorage()
    if (stored) {
      state.value.analytics = stored.analytics ? 'granted' : 'denied'
      state.value.needsChoice = false
    } else {
      state.value.analytics = 'pending'
      state.value.needsChoice = true
    }
    state.value.isLoaded = true
  }

  function accept() {
    writeStorage(true)
    state.value.analytics = 'granted'
    state.value.needsChoice = false
  }

  function refuse() {
    writeStorage(false)
    state.value.analytics = 'denied'
    state.value.needsChoice = false
  }

  function openModal() {
    if (typeof window !== 'undefined') {
      window.dispatchEvent(new CustomEvent('open-cookie-settings'))
    }
  }

  return { state, accept, refuse, openModal }
}
```

Notes :
- `useState` est l'API SSR-safe de Nuxt pour partager du state réactif. Au render SSR, `analytics: 'pending'`, `needsChoice: true` (cohérent avec premier visiteur). L'init côté client mute le state après mount.
- Le banner ne sera rendu qu'avec `<ClientOnly>` pour éviter un mismatch d'hydratation (Task 7).
- Pas de `readonly()` pour laisser les watchers de plugins muter via les actions exposées.

- [ ] **Step 2: Vérifier la compilation**

```bash
npm run dev
```

Ouvre la console du navigateur. Tape :

```js
// Pas de useConsent global, mais on peut tester via le runtime Vue.
// Si le composable charge sans erreur, la build est OK.
useNuxtApp().$config.public.gtagId
```

**Attendu :** `'G-KTGGEP09JY'`. Pas d'erreur de compilation TypeScript dans la console dev. Pas de page blanche.

- [ ] **Step 3: Commit**

```bash
git add app/composables/useConsent.ts
git commit -m "feat(consent): add useConsent composable with 13mo TTL"
```

---

## Task 3 : Plugin `gtag.client.ts`

**Files:**
- Create: `/Users/mathieu/Documents/survivor/app/plugins/gtag.client.ts`

Charge gtag.js, pose `consent default denied` avant `config`, met à jour vers `granted` synchronement si localStorage l'indique, watch les changements.

- [ ] **Step 1: Créer le plugin**

Crée `app/plugins/gtag.client.ts` :

```ts
// app/plugins/gtag.client.ts
// Charge GA4 avec Google Consent Mode v2.
// Voir docs/superpowers/specs/2026-05-16-cookies-ga4-design.md §Flow de consentement.

declare global {
  interface Window {
    dataLayer: unknown[]
    gtag: (...args: unknown[]) => void
  }
}

export default defineNuxtPlugin(() => {
  const config = useRuntimeConfig()
  const gtagId = config.public.gtagId as string

  if (!gtagId) {
    if (import.meta.dev) console.warn('[gtag] NUXT_PUBLIC_GTAG_ID missing, analytics disabled')
    return
  }

  // 1. Setup dataLayer + gtag avant tout appel.
  window.dataLayer = window.dataLayer || []
  window.gtag = function gtag() {
    // Conserve la pattern officielle Google (push(arguments)).
    // eslint-disable-next-line prefer-rest-params
    window.dataLayer.push(arguments)
  }

  // 2. Consent default : tout en denied, fenêtre 500ms pour update synchrone.
  window.gtag('consent', 'default', {
    analytics_storage: 'denied',
    ad_storage: 'denied',
    ad_user_data: 'denied',
    ad_personalization: 'denied',
    wait_for_update: 500,
  })

  // 3. Init GA4. anonymize_ip pour réduire l'empreinte légale.
  window.gtag('js', new Date())
  window.gtag('config', gtagId, { anonymize_ip: true })

  // 4. Charge le script gtag.js (async).
  const script = document.createElement('script')
  script.async = true
  script.src = `https://www.googletagmanager.com/gtag/js?id=${gtagId}`
  document.head.appendChild(script)

  // 5. Si l'utilisateur a déjà accepté (visite de retour), update synchrone
  //    AVANT que les 500ms wait_for_update n'expirent.
  const { state } = useConsent()
  if (state.value.analytics === 'granted') {
    window.gtag('consent', 'update', {
      analytics_storage: 'granted',
      ad_storage: 'granted',
      ad_user_data: 'granted',
      ad_personalization: 'granted',
    })
  }

  // 6. Watch les changements ultérieurs.
  watch(() => state.value.analytics, (next) => {
    const granted = next === 'granted'
    window.gtag('consent', 'update', {
      analytics_storage: granted ? 'granted' : 'denied',
      ad_storage: granted ? 'granted' : 'denied',
      ad_user_data: granted ? 'granted' : 'denied',
      ad_personalization: granted ? 'granted' : 'denied',
    })
  })
})
```

- [ ] **Step 2: Vérifier le chargement gtag en dev**

```bash
npm run dev
```

Sur `http://localhost:3000` en navigation privée (incognito) :

1. Ouvre DevTools > Network > filtre par `gtag`. Recharge la page.
   **Attendu :** une requête vers `https://www.googletagmanager.com/gtag/js?id=G-KTGGEP09JY` qui retourne 200.

2. Dans la console :

```js
window.dataLayer.slice(0, 5)
```

**Attendu :** 4 entrées au moins. Une `'consent', 'default', {...}` avec `analytics_storage: 'denied'`, puis `'js', Date`, puis `'config', 'G-KTGGEP09JY', {anonymize_ip: true}`. Le premier élément est typiquement la commande consent.

3. Application > Cookies : **aucun cookie `_ga*`** présent (denied par défaut).

- [ ] **Step 3: Vérifier le path "accept stocké" (return visitor)**

Dans la console :

```js
localStorage.setItem('cookie-consent', JSON.stringify({analytics: true, timestamp: Date.now(), version: 1}))
location.reload()
```

Après reload :

1. Application > Cookies : présence de **`_ga` et `_ga_KTGGEP09JY`**.
2. Console :

```js
window.dataLayer.find(e => e[0] === 'consent' && e[1] === 'update')
```

**Attendu :** retourne une commande `['consent', 'update', { analytics_storage: 'granted', ... }]`.

Nettoie ensuite :

```js
localStorage.removeItem('cookie-consent')
```

- [ ] **Step 4: Commit**

```bash
git add app/plugins/gtag.client.ts
git commit -m "feat(analytics): add gtag.js plugin with Consent Mode v2"
```

---

## Task 4 : Patch `posthog.client.ts`

**Files:**
- Modify: `/Users/mathieu/Documents/survivor/app/plugins/posthog.client.ts`

PostHog init lit `useConsent` synchrone : si `granted` → mode complet direct ; sinon → mode cookieless actuel. Watcher pour les changements.

- [ ] **Step 1: Réécrire le plugin**

Remplace **tout le contenu** de `app/plugins/posthog.client.ts` par :

```ts
// app/plugins/posthog.client.ts
// PostHog : mode cookieless par défaut, full mode (cookies + session recording) sous consentement.
// Voir docs/superpowers/specs/2026-05-16-cookies-ga4-design.md §Flow de consentement.

import posthog from 'posthog-js'

export default defineNuxtPlugin(() => {
  const config = useRuntimeConfig()
  const key = config.public.posthogKey as string

  if (!key) {
    if (import.meta.dev) console.warn('[posthog] NUXT_PUBLIC_POSTHOG_KEY missing, analytics disabled')
    return { provide: { posthog: null as null | typeof posthog } }
  }

  const { state } = useConsent()
  const initialGranted = state.value.analytics === 'granted'

  posthog.init(key, {
    api_host: '/_ph',
    ui_host: 'https://eu.posthog.com',
    // Init selon consentement stocké : full mode si granted, sinon cookieless.
    persistence: initialGranted ? 'localStorage+cookie' : 'memory',
    disable_persistence: !initialGranted,
    disable_session_recording: !initialGranted,
    enable_recording_console_log: false,
    capture_pageview: false,
    capture_pageleave: true,
    autocapture: true,
    capture_performance: true,
    respect_dnt: true,
    loaded: (ph) => {
      try {
        if (typeof window !== 'undefined' && window.localStorage?.getItem('ph_optout') === '1') {
          ph.opt_out_capturing()
        }
      } catch { /* localStorage may be blocked, ignore */ }
    },
  })

  // Watch les changements de consentement pour basculer entre les modes.
  watch(() => state.value.analytics, (next) => {
    if (next === 'granted') {
      posthog.set_config({
        persistence: 'localStorage+cookie',
        disable_persistence: false,
        disable_session_recording: false,
      })
      posthog.startSessionRecording()
    } else {
      // Refus ou révocation : on stoppe le replay et on coupe la persistance.
      // Les cookies déjà déposés ne sont pas effacés (limitation documentée dans /cookies).
      posthog.stopSessionRecording()
      posthog.set_config({
        persistence: 'memory',
        disable_persistence: true,
        disable_session_recording: true,
      })
    }
  })

  // Pageview manuel sur chaque navigation (inchangé).
  const router = useRouter()
  const unsubscribe = router.afterEach((to) => {
    posthog.capture('$pageview', { $current_url: window.location.origin + to.fullPath })
  })

  if (import.meta.hot) {
    import.meta.hot.dispose(() => unsubscribe())
  }

  return {
    provide: {
      posthog,
    },
  }
})
```

- [ ] **Step 2: Vérifier le mode cookieless (refuseur ou pending)**

```bash
npm run dev
```

Navigation privée. Sur `http://localhost:3000` :

1. Application > Cookies : **aucun cookie `ph_*`** (mode memory).
2. Application > Local Storage : **aucune clé `ph_*`** non plus.
3. Network : les events PostHog partent quand même vers `/_ph/...` (mesure d'audience anonyme).

- [ ] **Step 3: Vérifier le mode full (accepteur)**

Dans la console :

```js
localStorage.setItem('cookie-consent', JSON.stringify({analytics: true, timestamp: Date.now(), version: 1}))
location.reload()
```

Après reload :

1. Application > Cookies : présence de **cookies `ph_*`** (typiquement `ph_phc_*_posthog`).
2. Application > Local Storage : présence d'entrées **`ph_*`**.
3. Console :

```js
useNuxtApp().$posthog?.config.disable_session_recording
```

**Attendu :** `false`.

Nettoie :

```js
localStorage.clear()
```

- [ ] **Step 4: Commit**

```bash
git add app/plugins/posthog.client.ts
git commit -m "feat(analytics): gate PostHog full mode behind consent, keep cookieless fallback"
```

---

## Task 5 : Composant `CookieBanner.vue`

**Files:**
- Create: `/Users/mathieu/Documents/survivor/app/components/CookieBanner.vue`

Composant unique contenant le bandeau slide-up bas **et** la modal de personnalisation. Écoute l'événement `open-cookie-settings`. Utilise `useConsent` pour lire l'état et appeler `accept()` / `refuse()`. SVG cookie menthe inline.

- [ ] **Step 1: Créer le composant**

Crée `app/components/CookieBanner.vue` :

```vue
<!-- app/components/CookieBanner.vue -->
<!-- Bandeau de consentement RGPD + modal personnalisation.
     Voir docs/superpowers/specs/2026-05-16-cookies-ga4-design.md §UX & DA. -->
<script setup lang="ts">
const { state, accept, refuse } = useConsent()

const showModal = ref(false)
const draftAnalytics = ref(false)

// Synchroniser le toggle quand la modal s'ouvre avec l'état courant.
function openModalFromBanner() {
  draftAnalytics.value = state.value.analytics === 'granted'
  showModal.value = true
}

function openModalFromReopen() {
  draftAnalytics.value = state.value.analytics === 'granted'
  showModal.value = true
}

function closeModal() {
  showModal.value = false
}

function acceptAll() {
  accept()
  showModal.value = false
}

function refuseAll() {
  refuse()
  showModal.value = false
}

function saveCustom() {
  if (draftAnalytics.value) accept()
  else refuse()
  showModal.value = false
}

// Écoute la réouverture depuis le footer ou /cookies.
onMounted(() => {
  window.addEventListener('open-cookie-settings', openModalFromReopen)
})
onBeforeUnmount(() => {
  window.removeEventListener('open-cookie-settings', openModalFromReopen)
})
</script>

<template>
  <div>
    <!-- Bandeau slide-up bas -->
    <Transition name="slide-up">
      <div
        v-if="state.needsChoice && !showModal"
        class="cookie-banner"
        role="dialog"
        aria-labelledby="cookie-banner-kicker"
        aria-describedby="cookie-banner-text"
      >
        <div class="cookie-banner-inner container">
          <div class="cookie-banner-content">
            <svg class="cookie-svg" viewBox="0 0 32 32" aria-hidden="true">
              <!-- Cercle principal + encoche bas-droite -->
              <path
                d="M16 3 A13 13 0 1 0 28 22 a5 5 0 0 1 -5 -5 a5 5 0 0 1 -5 -5 A6 6 0 0 1 16 3 z"
                fill="none"
                stroke="#6CE3B5"
                stroke-width="1.5"
              />
              <!-- Pépites -->
              <circle cx="11" cy="11" r="1.4" fill="#6CE3B5" fill-opacity="0.6" />
              <circle cx="20" cy="14" r="1.2" fill="#6CE3B5" fill-opacity="0.6" />
              <circle cx="13" cy="20" r="1.6" fill="#6CE3B5" fill-opacity="0.6" />
              <circle cx="19" cy="22" r="1.0" fill="#6CE3B5" fill-opacity="0.6" />
            </svg>
            <div class="cookie-banner-text">
              <KickerLabel id="cookie-banner-kicker">POLITIQUE COOKIES</KickerLabel>
              <p id="cookie-banner-text">
                Ce site utilise des cookies pour mesurer l'audience et améliorer l'expérience.
                <NuxtLink to="/cookies" class="cookie-link">Détails</NuxtLink>
              </p>
            </div>
          </div>
          <div class="cookie-banner-actions">
            <button
              type="button"
              class="cookie-btn cookie-btn-ghost"
              data-attr="cookie-refuse"
              @click="refuseAll"
            >
              Refuser
            </button>
            <button
              type="button"
              class="cookie-btn cookie-btn-outline"
              data-attr="cookie-customize"
              @click="openModalFromBanner"
            >
              Personnaliser
            </button>
            <button
              type="button"
              class="cookie-btn cookie-btn-primary"
              data-attr="cookie-accept"
              @click="acceptAll"
            >
              Accepter
            </button>
          </div>
        </div>
      </div>
    </Transition>

    <!-- Modal personnalisation -->
    <Transition name="fade">
      <div
        v-if="showModal"
        class="cookie-modal-overlay"
        role="dialog"
        aria-modal="true"
        aria-labelledby="cookie-modal-title"
        @click.self="closeModal"
      >
        <div class="cookie-modal-card">
          <div class="cookie-modal-header">
            <KickerLabel>PRÉFÉRENCES COOKIES</KickerLabel>
            <h2 id="cookie-modal-title">Vos préférences cookies</h2>
            <button
              type="button"
              class="cookie-modal-close"
              aria-label="Fermer"
              data-attr="cookie-modal-close"
              @click="closeModal"
            >
              <svg viewBox="0 0 16 16" width="16" height="16" aria-hidden="true">
                <path d="M2 2 L14 14 M14 2 L2 14" stroke="currentColor" stroke-width="1.5" />
              </svg>
            </button>
          </div>

          <p class="cookie-modal-intro">
            Vous gardez le contrôle. Vos choix s'appliquent immédiatement et peuvent être modifiés à tout moment.
          </p>

          <div class="cookie-toggle-row cookie-toggle-disabled">
            <div class="cookie-toggle-meta">
              <h3>Cookies essentiels</h3>
              <p>
                Nécessaires au fonctionnement du site : mémorisation de vos choix cookies, indicateur scanner.
                Aucune donnée comportementale.
              </p>
            </div>
            <div class="cookie-toggle-side">
              <div class="cookie-toggle on" aria-hidden="true">
                <span class="cookie-toggle-knob" />
              </div>
              <span class="cookie-toggle-label">Toujours actifs</span>
            </div>
          </div>

          <div class="cookie-toggle-row">
            <div class="cookie-toggle-meta">
              <h3>Mesure d'audience et replays</h3>
              <p>
                Google Analytics 4 (mesure d'audience, attribution Google Ads) et PostHog
                (parcours, enregistrements de sessions, heatmaps). Données pseudonymisées.
                Hébergement : Europe pour PostHog, États-Unis pour Google (transferts encadrés
                par clauses contractuelles types).
              </p>
              <NuxtLink to="/cookies" class="cookie-link">Détail complet sur la page Cookies →</NuxtLink>
            </div>
            <div class="cookie-toggle-side">
              <button
                type="button"
                class="cookie-toggle"
                :class="{ on: draftAnalytics }"
                role="switch"
                :aria-checked="draftAnalytics"
                data-attr="cookie-toggle-analytics"
                @click="draftAnalytics = !draftAnalytics"
              >
                <span class="cookie-toggle-knob" />
              </button>
            </div>
          </div>

          <div class="cookie-modal-footer">
            <button
              type="button"
              class="cookie-btn cookie-btn-ghost"
              data-attr="cookie-modal-cancel"
              @click="closeModal"
            >
              Annuler
            </button>
            <button
              type="button"
              class="cookie-btn cookie-btn-primary"
              data-attr="cookie-modal-save"
              @click="saveCustom"
            >
              Enregistrer mes choix
            </button>
          </div>
        </div>
      </div>
    </Transition>
  </div>
</template>

<style scoped>
/* === Bandeau === */
.cookie-banner {
  position: fixed;
  bottom: 0; left: 0; right: 0;
  z-index: 100;
  background: rgba(20, 20, 15, 0.95); /* var(--color-surface) à 95% */
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  border-top: 1px solid var(--color-accent);
  padding: 1.25rem 0;
}
.cookie-banner-inner {
  display: grid;
  grid-template-columns: 1fr auto;
  gap: 2rem;
  align-items: center;
}
.cookie-banner-content {
  display: flex;
  align-items: flex-start;
  gap: 1rem;
}
.cookie-svg {
  width: 32px; height: 32px;
  flex-shrink: 0;
  transition: transform 400ms ease;
}
.cookie-banner:hover .cookie-svg { transform: rotate(4deg); }
.cookie-banner-text p {
  margin: 0.4rem 0 0;
  font-size: 0.85rem;
  color: var(--color-muted);
  line-height: 1.55;
  max-width: 60ch;
}
.cookie-link {
  color: var(--color-accent);
  text-decoration: underline;
  text-underline-offset: 2px;
  margin-left: 0.25rem;
}
.cookie-banner-actions {
  display: flex;
  gap: 0.6rem;
  flex-wrap: nowrap;
}

/* === Boutons (3 styles) === */
.cookie-btn {
  font-family: inherit;
  font-size: 0.85rem;
  padding: 0.6rem 1.25rem;
  border-radius: 2px;
  cursor: pointer;
  transition: all 0.15s;
  white-space: nowrap;
  background: transparent;
  border: 1px solid transparent;
}
.cookie-btn-ghost {
  border-color: var(--color-rule);
  color: var(--color-muted);
}
.cookie-btn-ghost:hover { color: var(--color-text); border-color: var(--color-muted); }
.cookie-btn-outline {
  border-color: var(--color-accent);
  color: var(--color-accent);
}
.cookie-btn-outline:hover { background: var(--color-accent-soft); }
.cookie-btn-primary {
  background: var(--color-accent);
  color: #0F0F0E;
  font-weight: 500;
  border-color: var(--color-accent);
}
.cookie-btn-primary:hover { filter: brightness(1.05); }

/* === Modal === */
.cookie-modal-overlay {
  position: fixed; inset: 0;
  z-index: 110;
  background: rgba(0, 0, 0, 0.7);
  backdrop-filter: blur(4px);
  display: flex; align-items: center; justify-content: center;
  padding: 1rem;
}
.cookie-modal-card {
  background: var(--color-surface-2);
  border: 1px solid var(--color-rule);
  border-radius: 2px;
  max-width: 540px;
  width: 100%;
  max-height: 90vh;
  overflow-y: auto;
  padding: 2rem;
}
.cookie-modal-header { position: relative; margin-bottom: 1rem; }
.cookie-modal-header h2 {
  font-size: 1.25rem;
  margin: 0.5rem 0 0;
  color: var(--color-text);
}
.cookie-modal-close {
  position: absolute; top: 0; right: 0;
  background: transparent; border: none; cursor: pointer;
  color: var(--color-muted);
  padding: 0.4rem;
  transition: color 0.15s;
}
.cookie-modal-close:hover { color: var(--color-accent); }
.cookie-modal-intro {
  font-size: 0.85rem;
  color: var(--color-muted);
  line-height: 1.55;
  margin: 0 0 1.5rem;
}

/* === Toggle rows === */
.cookie-toggle-row {
  display: grid;
  grid-template-columns: 1fr auto;
  gap: 1.5rem;
  padding: 1rem 0;
  border-top: 1px solid var(--color-rule);
}
.cookie-toggle-meta h3 {
  font-size: 0.95rem;
  color: var(--color-text);
  margin: 0 0 0.4rem;
}
.cookie-toggle-meta p {
  font-size: 0.8rem;
  color: var(--color-muted);
  line-height: 1.55;
  margin: 0 0 0.4rem;
}
.cookie-toggle-side {
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  gap: 0.4rem;
}
.cookie-toggle-label {
  font-size: 0.7rem;
  color: var(--color-muted-soft);
  font-family: var(--font-mono);
  letter-spacing: 0.08em;
  text-transform: uppercase;
}
.cookie-toggle {
  position: relative;
  width: 36px; height: 20px;
  background: var(--color-rule);
  border: none;
  border-radius: 999px;
  cursor: pointer;
  transition: background 200ms;
  padding: 0;
}
.cookie-toggle.on { background: var(--color-accent); }
.cookie-toggle-knob {
  position: absolute;
  top: 2px; left: 2px;
  width: 16px; height: 16px;
  background: white;
  border-radius: 50%;
  transition: transform 200ms;
}
.cookie-toggle.on .cookie-toggle-knob { transform: translateX(16px); }
.cookie-toggle-disabled .cookie-toggle { opacity: 0.4; cursor: not-allowed; }

.cookie-modal-footer {
  display: flex;
  justify-content: flex-end;
  gap: 0.75rem;
  margin-top: 1.5rem;
  border-top: 1px solid var(--color-rule);
  padding-top: 1.5rem;
}

/* === Transitions === */
.slide-up-enter-active, .slide-up-leave-active {
  transition: transform 350ms cubic-bezier(.2, .8, .2, 1);
}
.slide-up-enter-from, .slide-up-leave-to { transform: translateY(100%); }
.fade-enter-active, .fade-leave-active { transition: opacity 250ms ease; }
.fade-enter-from, .fade-leave-to { opacity: 0; }

/* === Mobile === */
@media (max-width: 768px) {
  .cookie-banner-inner {
    grid-template-columns: 1fr;
    gap: 1.25rem;
  }
  .cookie-banner-actions {
    flex-direction: column-reverse;
  }
  .cookie-btn {
    width: 100%;
  }
  .cookie-modal-card {
    padding: 1.5rem;
  }
  .cookie-toggle-row {
    grid-template-columns: 1fr;
    gap: 0.75rem;
  }
  .cookie-toggle-side {
    flex-direction: row;
    align-items: center;
    justify-content: flex-start;
  }
}

@media (prefers-reduced-motion: reduce) {
  .slide-up-enter-active, .slide-up-leave-active, .fade-enter-active, .fade-leave-active,
  .cookie-toggle, .cookie-toggle-knob, .cookie-svg {
    transition: none !important;
  }
  .cookie-banner:hover .cookie-svg { transform: none; }
}
</style>
```

Notes :
- `data-attr="cookie-*"` permet à PostHog autocapture de tagger les interactions (cohérent avec le pattern AppFooter).
- `role="dialog"` + `aria-modal="true"` sur la modal pour accessibilité.
- Le SVG cookie est inline (~20 lignes), pas de dépendance Icon.

- [ ] **Step 2: Vérifier (la mount globale est dans Task 7, ici on s'assure juste que le composant compile)**

```bash
npm run dev
```

Pas d'erreur de compilation Vue/TypeScript dans la console dev. Le bandeau n'est pas encore visible (pas monté), c'est attendu.

- [ ] **Step 3: Commit**

```bash
git add app/components/CookieBanner.vue
git commit -m "feat(consent): add CookieBanner component (slide-up + modal)"
```

---

## Task 6 : Monter `CookieBanner` globalement

**Files:**
- Modify: `/Users/mathieu/Documents/survivor/app/app.vue`

Le bandeau doit apparaître sur toutes les pages, hors SSR. On le place dans `app.vue` (au-dessus de `NuxtLayout` ou en sibling de `NuxtPage`).

- [ ] **Step 1: Ajouter `<ClientOnly><CookieBanner /></ClientOnly>` dans le template d'`app.vue`**

Le template actuel est :

```vue
<template>
  <NuxtLayout>
    <NuxtPage />
  </NuxtLayout>
</template>
```

Vue 3 supporte les fragments (plusieurs racines dans `<template>`). Remplace-le par :

```vue
<template>
  <NuxtLayout>
    <NuxtPage />
  </NuxtLayout>
  <ClientOnly>
    <CookieBanner />
  </ClientOnly>
</template>
```

`<ClientOnly>` évite tout problème d'hydratation (l'état `needsChoice` peut changer entre SSR et client après lecture de localStorage). Pas besoin d'import explicite : Nuxt auto-importe `CookieBanner` depuis `~/components/` (config `pathPrefix: false` dans `nuxt.config.ts`).

- [ ] **Step 2: Vérifier en navigation privée**

```bash
npm run dev
```

`http://localhost:3000` en incognito :
- Le bandeau slide-up apparaît en bas de page (transition 350ms).
- Clic « Personnaliser » → la modal s'ouvre par-dessus, bandeau caché.
- Clic « Annuler » dans la modal → modal se ferme, **bandeau réapparaît** (état toujours `needsChoice`).
- Clic « Accepter » → bandeau et modal disparaissent. localStorage `cookie-consent` créé. Reload : bandeau ne réapparaît pas.
- Cookies `_ga*` et `ph_*` apparaissent (Tasks 3+4 actives).

- [ ] **Step 3: Vérifier mobile (viewport DevTools)**

DevTools > Toggle device toolbar > iPhone 12 Pro (390×844). Le bandeau passe en layout vertical, 3 boutons empilés pleine largeur, primaire en haut (visuellement, donc en bas du DOM via `column-reverse`).

- [ ] **Step 4: Vérifier `prefers-reduced-motion`**

DevTools > Rendering > Emulate CSS media feature `prefers-reduced-motion: reduce`. Reload. Pas d'animation slide-up, juste apparition immédiate. Pas de rotation du SVG cookie au hover.

- [ ] **Step 5: Nettoyer le localStorage et commit**

Console :

```js
localStorage.clear()
```

```bash
git add app/app.vue
git commit -m "feat(consent): mount CookieBanner globally via ClientOnly"
```

---

## Task 7 : Section "Légal" dans `AppFooter`

**Files:**
- Modify: `/Users/mathieu/Documents/survivor/app/components/AppFooter.vue`

Le footer actuel n'a pas de liens légaux. On ajoute une 4e colonne (ou on remplace la grille) avec :
- Politique de confidentialité (`/confidentialite`)
- Cookies (`/cookies`)
- Gérer mes cookies (bouton qui dispatch `open-cookie-settings`)

- [ ] **Step 1: Élargir la grille footer + ajouter la colonne Légal**

Édite `app/components/AppFooter.vue`. Remplace le bloc `footer-links` (lignes 17-21) par deux colonnes : la navigation existante + la nouvelle légale.

Remplace :

```vue
      <div class="footer-links">
        <NuxtLink to="/rapports" data-attr="footer-nav-rapports">Rapports de Survie</NuxtLink>
        <NuxtLink to="/frequence" data-attr="footer-nav-frequence">Rejoindre la Fréquence</NuxtLink>
        <NuxtLink to="/identite" data-attr="footer-nav-identite">Identité du Survivant</NuxtLink>
      </div>
```

par :

```vue
      <div class="footer-links">
        <NuxtLink to="/rapports" data-attr="footer-nav-rapports">Rapports de Survie</NuxtLink>
        <NuxtLink to="/frequence" data-attr="footer-nav-frequence">Rejoindre la Fréquence</NuxtLink>
        <NuxtLink to="/identite" data-attr="footer-nav-identite">Identité du Survivant</NuxtLink>
      </div>

      <div class="footer-links footer-links-legal">
        <NuxtLink to="/confidentialite" data-attr="footer-legal-confidentialite">Politique de confidentialité</NuxtLink>
        <NuxtLink to="/cookies" data-attr="footer-legal-cookies">Cookies</NuxtLink>
        <button
          type="button"
          class="footer-legal-button"
          data-attr="footer-legal-manage-cookies"
          @click="openCookieSettings"
        >
          Gérer mes cookies
        </button>
      </div>
```

- [ ] **Step 2: Ajouter la fonction `openCookieSettings` dans `<script setup>`**

Remplace le bloc `<script setup>` :

```vue
<script setup lang="ts">
const socials = {
  linkedin: '',
  instagram: '',
  youtube: '',
}
</script>
```

par :

```vue
<script setup lang="ts">
const socials = {
  linkedin: '',
  instagram: '',
  youtube: '',
}

function openCookieSettings() {
  if (typeof window !== 'undefined') {
    window.dispatchEvent(new CustomEvent('open-cookie-settings'))
  }
}
</script>
```

- [ ] **Step 3: Ajuster la grille pour 4 colonnes (brand + nav + légal + social)**

Édite le bloc `<style scoped>`. Remplace :

```css
.footer-inner {
  display: grid;
  grid-template-columns: 1fr auto auto;
  gap: 3rem;
  align-items: start;
  margin-bottom: 2rem;
}
```

par :

```css
.footer-inner {
  display: grid;
  grid-template-columns: 1fr auto auto auto;
  gap: 3rem;
  align-items: start;
  margin-bottom: 2rem;
}
@media (max-width: 768px) {
  .footer-inner {
    grid-template-columns: 1fr;
    gap: 2rem;
  }
}
```

Et ajoute, à la fin du `<style scoped>` (avant `</style>`) :

```css
.footer-legal-button {
  font-size: 0.8rem;
  font-family: var(--font-mono);
  color: var(--color-muted);
  background: transparent;
  border: none;
  padding: 0;
  cursor: pointer;
  text-align: left;
  transition: color 0.15s;
}
.footer-legal-button:hover { color: var(--color-accent); }
```

(Les styles `.footer-links a` existants s'appliquent déjà aux NuxtLinks.)

- [ ] **Step 4: Vérifier en dev**

```bash
npm run dev
```

Sur n'importe quelle page :
- Le footer affiche la nouvelle colonne « Légal » avec 3 entrées (« Politique de confidentialité », « Cookies », « Gérer mes cookies »).
- Clic sur « Politique de confidentialité » → route vers `/confidentialite` (existe déjà).
- Clic sur « Cookies » → route vers `/cookies` → **404** (page créée en Task 8, on s'attend à ça).
- Clic sur « Gérer mes cookies » → **la modal du CookieBanner s'ouvre** (toggle pré-coché selon état courant).
- Mobile (viewport <768px) : les colonnes s'empilent verticalement.

- [ ] **Step 5: Commit**

```bash
git add app/components/AppFooter.vue
git commit -m "feat(footer): add legal section with cookies management"
```

---

## Task 8 : Page `/cookies`

**Files:**
- Create: `/Users/mathieu/Documents/survivor/app/pages/cookies.vue`
- Modify: `/Users/mathieu/Documents/survivor/nuxt.config.ts` (sitemap exclusion + prerender)

Page éditoriale détaillant chaque cookie. Reprend le langage visuel de `/confidentialite`. Bouton « Gérer mes cookies » dispatch l'événement.

- [ ] **Step 1: Créer la page**

Crée `app/pages/cookies.vue` :

```vue
<!-- app/pages/cookies.vue -->
<!-- Détail RGPD/LPD des cookies et traceurs.
     Voir docs/superpowers/specs/2026-05-16-cookies-ga4-design.md §UX & DA. -->
<script setup lang="ts">
useSeoMeta({
  title: 'Cookies et traceurs - Survivant-IA',
  description: 'Détail des cookies et traceurs utilisés sur survivant-ia.ch, conformément au RGPD et à la LPD suisse.',
  robots: 'noindex, follow',
})

defineOgImage(false)

function openCookieSettings() {
  if (typeof window !== 'undefined') {
    window.dispatchEvent(new CustomEvent('open-cookie-settings'))
  }
}
</script>

<template>
  <div class="container" style="padding-top: 4rem; padding-bottom: 6rem; max-width: 65ch;">
    <KickerLabel>POLITIQUE COOKIES</KickerLabel>
    <h1 class="cookies-h1">Cookies et <em>traceurs</em></h1>

    <section class="policy-section">
      <h2>Qu'est-ce qu'un cookie ?</h2>
      <p>
        Un cookie est un petit fichier déposé sur votre appareil par un site internet pour mémoriser
        des informations entre vos visites. Certains sont indispensables au fonctionnement du site,
        d'autres permettent de mesurer l'audience ou de personnaliser l'expérience.
      </p>
    </section>

    <section class="policy-section">
      <h2>Cookies essentiels</h2>
      <p>Toujours actifs. Aucune donnée comportementale n'est collectée.</p>

      <div class="cookie-detail">
        <h3>cookie-consent</h3>
        <ul>
          <li><strong>Type :</strong> localStorage</li>
          <li><strong>Finalité :</strong> Mémoriser votre choix de consentement aux cookies analytiques</li>
          <li><strong>Durée :</strong> 13 mois (renouvellement automatique conforme CNIL), ou jusqu'à effacement manuel</li>
          <li><strong>Éditeur :</strong> Survivant-IA</li>
        </ul>
      </div>

      <div class="cookie-detail">
        <h3>hasUnlockedScanner</h3>
        <ul>
          <li><strong>Type :</strong> localStorage</li>
          <li><strong>Finalité :</strong> Mémoriser que vous avez déverrouillé le rapport complet du scanner (éviter de redemander votre email)</li>
          <li><strong>Durée :</strong> Permanent, jusqu'à effacement manuel</li>
          <li><strong>Éditeur :</strong> Survivant-IA</li>
        </ul>
      </div>
    </section>

    <section class="policy-section">
      <h2>Mesure d'audience (consentement requis)</h2>
      <p>
        Ces cookies ne sont déposés que si vous acceptez via la bannière. Si vous refusez, les outils
        ci-dessous fonctionnent en mode anonymisé sans cookie.
      </p>

      <div class="cookie-detail">
        <h3>Google Analytics 4</h3>
        <ul>
          <li><strong>Noms :</strong> <code>_ga</code>, <code>_ga_KTGGEP09JY</code></li>
          <li><strong>Finalité :</strong> Mesure d'audience, attribution des sources de trafic, préparation d'éventuelles campagnes Google Ads</li>
          <li><strong>Durée :</strong> 13 mois (renouvellement de consentement)</li>
          <li><strong>Éditeur :</strong> Google LLC (États-Unis)</li>
          <li><strong>Transfert hors UE :</strong> Oui, États-Unis, encadré par les clauses contractuelles types (SCC) approuvées par la Commission européenne</li>
          <li><strong>Anonymisation :</strong> IP anonymisée (paramètre <code>anonymize_ip</code> actif)</li>
          <li><strong>Do Not Track :</strong> Non respecté nativement par Google. Votre choix dans la bannière prime.</li>
          <li><strong>Si vous refusez :</strong> Google Consent Mode v2 envoie des signaux agrégés sans cookie pour la modélisation d'attribution. Aucune identification individuelle.</li>
          <li><a href="https://policies.google.com/privacy" target="_blank" rel="noopener">Politique de confidentialité Google ↗</a></li>
        </ul>
      </div>

      <div class="cookie-detail">
        <h3>PostHog</h3>
        <ul>
          <li><strong>Noms :</strong> cookies <code>ph_*</code> (préfixe variable)</li>
          <li><strong>Finalité :</strong> Mesure d'audience, analyse de parcours, heatmaps, enregistrements de sessions</li>
          <li><strong>Durée :</strong> 1 an</li>
          <li><strong>Éditeur :</strong> PostHog Inc.</li>
          <li><strong>Hébergement :</strong> Union européenne (Francfort)</li>
          <li><strong>Do Not Track :</strong> Respecté automatiquement (PostHog ne capture rien si DNT est actif)</li>
          <li><strong>Si vous refusez :</strong> PostHog repasse en mode anonyme sans cookie ni enregistrement de session (mesure d'audience agrégée uniquement)</li>
          <li><a href="https://posthog.com/privacy" target="_blank" rel="noopener">Politique de confidentialité PostHog ↗</a></li>
        </ul>
      </div>
    </section>

    <section class="policy-section">
      <h2>Cookies marketing</h2>
      <p><em>Aucun cookie marketing n'est actuellement utilisé sur ce site.</em></p>
      <p style="font-size: 0.85rem;">
        Si nous activons des services publicitaires à l'avenir (par exemple Google Ads conversion tracking),
        cette section sera mise à jour et un nouveau consentement vous sera demandé.
      </p>
    </section>

    <section class="policy-section">
      <h2>Gérer mes préférences</h2>
      <p>Vous pouvez modifier votre choix à tout moment.</p>
      <button
        type="button"
        class="cookies-manage-btn"
        data-attr="cookies-page-manage"
        @click="openCookieSettings"
      >
        Gérer mes cookies
      </button>
    </section>

    <section class="policy-section">
      <h2>Vos droits (RGPD + LPD suisse)</h2>
      <ul>
        <li>Droit d'accès à vos données</li>
        <li>Droit de rectification</li>
        <li>Droit à l'effacement</li>
        <li>Droit à la limitation du traitement</li>
        <li>Droit d'opposition</li>
        <li>Droit à la portabilité</li>
      </ul>
      <p>
        Pour exercer ces droits, contactez :
        <a href="mailto:mathieu@survivant-ia.ch">mathieu@survivant-ia.ch</a>.
      </p>
    </section>

    <section class="policy-section">
      <h2>Limite technique</h2>
      <p>
        Si vous révoquez votre consentement après avoir accepté, les cookies déjà déposés ne sont
        pas automatiquement effacés (limitation technique). Vous pouvez les supprimer manuellement
        via les paramètres de votre navigateur (section « Données du site »).
      </p>
    </section>

    <p class="font-mono" style="font-size: 0.65rem; color: var(--color-muted); margin-top: 3rem;">
      Dernière mise à jour : mai 2026
    </p>
  </div>
</template>

<style scoped>
.cookies-h1 { margin: 0.5rem 0 2.5rem; }
.cookies-h1 em {
  font-family: 'Playfair Display', serif;
  font-style: italic;
  color: var(--color-accent);
  font-weight: 500;
}
.policy-section { margin-bottom: 2.5rem; }
.policy-section h2 {
  font-size: 1rem;
  margin: 0 0 0.75rem;
  color: var(--color-accent);
}
.policy-section h3 {
  font-size: 0.95rem;
  margin: 1rem 0 0.4rem;
  color: var(--color-text);
}
.policy-section p, .policy-section li {
  font-size: 0.9rem;
  color: var(--color-muted);
  line-height: 1.7;
}
.policy-section ul { padding-left: 1.5rem; margin: 0.5rem 0; }
.policy-section li { margin-bottom: 0.25rem; }
.policy-section code {
  font-family: var(--font-mono);
  font-size: 0.8rem;
  background: var(--color-surface-2);
  padding: 0.1rem 0.3rem;
  border-radius: 2px;
}
.cookie-detail {
  margin: 1.5rem 0;
  padding-left: 1rem;
  border-left: 2px solid var(--color-rule);
}
.cookies-manage-btn {
  background: var(--color-accent);
  color: #0F0F0E;
  font-weight: 500;
  border: 1px solid var(--color-accent);
  border-radius: 2px;
  padding: 0.75rem 1.5rem;
  font-size: 0.9rem;
  cursor: pointer;
  transition: filter 0.15s;
  margin-top: 0.5rem;
}
.cookies-manage-btn:hover { filter: brightness(1.05); }
</style>
```

- [ ] **Step 2: Mettre à jour `nuxt.config.ts` (sitemap + prerender)**

Édite `nuxt.config.ts` :

Dans `routeRules`, ajoute après la ligne `'/confidentialite': { sitemap: false }` :

```ts
    '/cookies': { sitemap: false },
```

Dans `nitro.prerender.routes`, ajoute `/cookies` à côté de `/confidentialite` (ligne ~120). Remplace :

```ts
        '/frequence', '/identite', '/confidentialite', '/chantier',
```

par :

```ts
        '/frequence', '/identite', '/confidentialite', '/cookies', '/chantier',
```

- [ ] **Step 3: Vérifier**

```bash
npm run dev
```

- Visite `http://localhost:3000/cookies` : page s'affiche avec le bon styling, h1 « Cookies et *traceurs* » (italique sur "traceurs"), 8 sections, bouton « Gérer mes cookies ».
- Clic sur « Gérer mes cookies » → la modal du CookieBanner s'ouvre.
- View source : `<meta name="robots" content="noindex, follow">` présent.

Build statique :

```bash
npm run build
```

Vérifie qu'aucune erreur de prerender pour `/cookies` n'apparaît dans les logs.

- [ ] **Step 4: Commit**

```bash
git add app/pages/cookies.vue nuxt.config.ts
git commit -m "feat(cookies): add /cookies page with detail and management button"
```

---

## Task 9 : Mise à jour `/confidentialite`

**Files:**
- Modify: `/Users/mathieu/Documents/survivor/app/pages/confidentialite.vue`

Supprimer la promesse "aucun cookie / DNT auto-respecté", réécrire la section Mesure d'audience, ajouter GA4, mettre à jour Sous-traitants et localStorage, dater mai 2026.

- [ ] **Step 1: Section "Données collectées" — élargir**

Édite `app/pages/confidentialite.vue`. Remplace le bloc lignes 23-32 :

```vue
    <section class="policy-section">
      <h2>Données collectées</h2>
      <p>Lors de l'inscription à la newsletter, nous collectons :</p>
      <ul>
        <li>Votre adresse email</li>
        <li>Votre prénom</li>
      </ul>
      <p>Au-delà de la newsletter, nous collectons des données d'audience anonymes (voir ci-dessous : <em>Mesure d'audience anonyme</em>).</p>
    </section>
```

par :

```vue
    <section class="policy-section">
      <h2>Données collectées</h2>
      <p>Selon votre interaction avec le site, nous collectons :</p>
      <ul>
        <li><strong>Inscription newsletter :</strong> adresse email, prénom</li>
        <li><strong>Mesure d'audience (sous consentement) :</strong> pages visitées, durée de visite, source de trafic, interactions, parcours de navigation, enregistrements de session, adresse IP anonymisée (Google Analytics 4)</li>
        <li><strong>Stockage technique local :</strong> indicateur de consentement cookies, indicateur scanner (voir <em>Stockage local</em> plus bas)</li>
      </ul>
      <p>Les outils de mesure d'audience (Google Analytics 4, PostHog) <strong>ne traitent jamais</strong> votre email ni votre prénom : ces données ne sont envoyées qu'à Brevo, qui gère la newsletter.</p>
    </section>
```

- [ ] **Step 2: Section "Mesure d'audience anonyme" — renommer et réécrire**

Remplace tout le bloc lignes 43-54 :

```vue
    <section class="policy-section">
      <h2>Mesure d'audience anonyme</h2>
      <p>Pour comprendre comment le site est utilisé et l'améliorer, nous utilisons <strong>PostHog</strong> (hébergé dans l'Union européenne, à Francfort) en mode strictement anonyme :</p>
      <ul>
        <li>Aucun cookie n'est déposé sur votre appareil</li>
        <li>Aucun identifiant persistant n'est créé</li>
        <li>Les données collectées (pages visitées, clics, métiers scannés, performances techniques) sont agrégées et ne permettent pas de vous identifier</li>
        <li>Conformément à l'exemption CNIL pour la mesure d'audience, ces données sont conservées 13 mois maximum</li>
      </ul>
      <p>Vous pouvez vous opposer à cette mesure en activant le mode <em>Do Not Track</em> de votre navigateur - nous le respectons automatiquement.</p>
      <p><a href="https://posthog.com/privacy" target="_blank" rel="noopener">Politique de confidentialité PostHog ↗</a></p>
    </section>
```

par :

```vue
    <section class="policy-section">
      <h2>Mesure d'audience</h2>
      <p>Pour comprendre comment le site est utilisé et l'améliorer, nous utilisons deux outils sous votre consentement explicite (bannière à la première visite, modifiable via « Gérer mes cookies » dans le footer ou la <NuxtLink to="/cookies">page Cookies</NuxtLink>) :</p>
      <ul>
        <li><strong>PostHog</strong> (hébergé dans l'Union européenne, à Francfort) : mesure de trafic, analyse de parcours, heatmaps, enregistrements de sessions. Conservation 1 an. <em>Do Not Track</em> respecté automatiquement. <a href="https://posthog.com/privacy" target="_blank" rel="noopener">Politique PostHog ↗</a></li>
        <li><strong>Google Analytics 4</strong> (Google LLC, États-Unis) : mesure d'audience, attribution des sources, préparation d'éventuelles campagnes Google Ads. Transfert hors UE encadré par les clauses contractuelles types (SCC) approuvées par la Commission européenne. IP anonymisée. Conservation 14 mois. <a href="https://policies.google.com/privacy" target="_blank" rel="noopener">Politique Google ↗</a></li>
      </ul>
      <p><strong>Si vous refusez</strong> : Google Analytics 4 fonctionne en mode anonymisé sans cookie (Google Consent Mode v2, signaux agrégés pour modélisation) ; PostHog repasse en mode anonyme sans cookie ni enregistrement de session.</p>
      <p>Le détail de chaque cookie (nom, durée, finalité) est disponible sur la <NuxtLink to="/cookies">page Cookies</NuxtLink>.</p>
    </section>
```

- [ ] **Step 3: Section "Sous-traitant" — élargir**

Remplace le bloc lignes 38-41 :

```vue
    <section class="policy-section">
      <h2>Sous-traitant</h2>
      <p>Les données sont traitées par <strong>Brevo</strong> (Sendinblue SAS), hébergé dans l'Union européenne, dans le respect du RGPD. <a href="https://www.brevo.com/legal/privacypolicy/" target="_blank" rel="noopener">Politique de confidentialité Brevo ↗</a></p>
    </section>
```

par :

```vue
    <section class="policy-section">
      <h2>Sous-traitants</h2>
      <p>Vos données sont traitées par les sous-traitants suivants, selon leur finalité :</p>
      <ul>
        <li><strong>Brevo (Sendinblue SAS)</strong> — newsletter, hébergement UE. <a href="https://www.brevo.com/legal/privacypolicy/" target="_blank" rel="noopener">Politique ↗</a></li>
        <li><strong>PostHog Inc.</strong> — mesure d'audience et heatmaps, hébergement UE (Francfort). <a href="https://posthog.com/privacy" target="_blank" rel="noopener">Politique ↗</a></li>
        <li><strong>Google LLC</strong> — Google Analytics 4, hébergement États-Unis avec clauses contractuelles types. <a href="https://policies.google.com/privacy" target="_blank" rel="noopener">Politique ↗</a></li>
      </ul>
      <p>Détail technique des cookies sur la <NuxtLink to="/cookies">page Cookies</NuxtLink>.</p>
    </section>
```

- [ ] **Step 4: Section "Stockage local" — ajouter sous-section consent**

Remplace le bloc lignes 56-68 (toute la section "Stockage local") :

```vue
    <section class="policy-section">
      <h2>Stockage local (localStorage)</h2>
      <h3>Indicateur technique du scanner</h3>
      <p>
        Lorsque vous déverrouillez le rapport complet du scanner via votre email,
        nous stockons un indicateur technique (<code>hasUnlockedScanner</code>) dans
        le <code>localStorage</code> de votre navigateur. Cet indicateur ne contient
        aucune donnée personnelle - il sert uniquement à éviter de vous redemander
        votre email lors de visites ultérieures depuis le même navigateur. Vous
        pouvez le supprimer à tout moment via les paramètres de votre navigateur
        (section "Données du site" ou "Stockage local").
      </p>
    </section>
```

par :

```vue
    <section class="policy-section">
      <h2>Stockage local (localStorage)</h2>

      <h3>Indicateur consentement cookies</h3>
      <p>
        Lorsque vous validez vos préférences cookies, nous stockons votre choix dans le
        <code>localStorage</code> sous la clé <code>cookie-consent</code>
        (<code>{ analytics, timestamp, version }</code>). Cette donnée ne quitte
        jamais votre navigateur. Elle est renouvelée tous les 13 mois, conformément
        à la recommandation CNIL.
      </p>

      <h3>Indicateur technique du scanner</h3>
      <p>
        Lorsque vous déverrouillez le rapport complet du scanner via votre email,
        nous stockons un indicateur technique (<code>hasUnlockedScanner</code>) dans
        le <code>localStorage</code> de votre navigateur. Cet indicateur ne contient
        aucune donnée personnelle - il sert uniquement à éviter de vous redemander
        votre email lors de visites ultérieures depuis le même navigateur. Vous
        pouvez le supprimer à tout moment via les paramètres de votre navigateur
        (section "Données du site" ou "Stockage local").
      </p>
    </section>
```

- [ ] **Step 5: Date — mai 2026**

Ligne 93. Remplace :

```vue
      Dernière mise à jour : avril 2026
```

par :

```vue
      Dernière mise à jour : mai 2026
```

- [ ] **Step 6: Vérifier**

```bash
npm run dev
```

`http://localhost:3000/confidentialite` :
- Plus aucune occurrence de « Aucun cookie n'est déposé », « mode strictement anonyme », « exemption CNIL ».
- Section « Mesure d'audience » liste PostHog ET Google Analytics 4.
- Section « Sous-traitants » au pluriel liste 3 entrées.
- Section « Stockage local » a deux sous-sections (`cookie-consent` + `hasUnlockedScanner`).
- Date « mai 2026 ».
- Liens vers `/cookies` cliquables et fonctionnels.

- [ ] **Step 7: Commit**

```bash
git add app/pages/confidentialite.vue
git commit -m "docs(privacy): update /confidentialite for GA4 + PostHog consent-gated mode"
```

---

## Task 10 : QA manuel + commit final

**Files:** aucun (vérification seulement)

Test exhaustif des 5 scénarios définis dans la spec. Aucun fichier modifié sauf si un bug est détecté.

- [ ] **Step 1: QA — premier visiteur (incognito)**

Ouvre une fenêtre incognito complètement neuve. Visite `http://localhost:3000` :

1. La bannière slide-up apparaît en bas (transition fluide).
2. DevTools > Application > Cookies : **aucun cookie `_ga*` ni `ph_*`** (denied par défaut).
3. DevTools > Application > Local Storage : **aucune clé `ph_*`** ni `cookie-consent`.
4. DevTools > Network : `gtag/js?id=G-KTGGEP09JY` chargé (200 OK).
5. Console : `window.dataLayer.find(e => e[1] === 'default')` retourne la commande consent default denied.

- [ ] **Step 2: QA — clic "Accepter"**

Toujours en incognito. Clique sur « Accepter » :

1. La bannière disparaît en slide-down.
2. Cookies : **`_ga`, `_ga_KTGGEP09JY` apparaissent**.
3. Local Storage : **clés `ph_*` apparaissent** + `cookie-consent` = `{"analytics":true,"timestamp":...,"version":1}`.
4. Console : `window.dataLayer.findLast(e => e[1] === 'update')` retourne une commande granted sur tous les storage.
5. Reload page → pas de bannière (consentement persisté).

- [ ] **Step 3: QA — clic "Refuser" (nouvelle session incognito)**

Nouvelle fenêtre incognito. Sur `/` :

1. Clique « Refuser ».
2. Bannière disparaît.
3. Cookies : **aucun `_ga*` ni `ph_*`**.
4. Local Storage : `cookie-consent` = `{"analytics":false,...}`. Aucune clé `ph_*`.
5. Console : `window.dataLayer.findLast(e => e[1] === 'update')` est `undefined` ou pointe vers du denied (jamais granted).
6. PostHog continue d'envoyer des events vers `/_ph/...` (mesure anonyme préservée).
7. Reload → pas de bannière.

- [ ] **Step 4: QA — clic "Personnaliser" + toggle**

Nouvelle fenêtre incognito :

1. Clique « Personnaliser » → la modal s'ouvre, toggle « Mesure d'audience » **désactivé** par défaut.
2. Active le toggle → fond menthe.
3. Clique « Enregistrer mes choix » → modal et bannière disparaissent.
4. Vérifie cookies + localStorage : équivalent à « Accepter ».

Recommence avec toggle laissé désactivé → équivalent à « Refuser ».

- [ ] **Step 5: QA — réouverture via footer**

Sur n'importe quelle page (état granted ou denied, peu importe) :

1. Scroll jusqu'au footer.
2. Clique sur « Gérer mes cookies » → la modal s'ouvre.
3. Le toggle reflète l'état actuel (coché si granted, décoché si denied).
4. Change le toggle → enregistre → l'état bascule. Vérifie cookies / localStorage.

- [ ] **Step 6: QA — réouverture via /cookies**

Visite `/cookies` :

1. La page s'affiche complètement, h1 « Cookies et *traceurs* », 8 sections.
2. Clique sur le bouton « Gérer mes cookies » dans la section dédiée → modal s'ouvre.

- [ ] **Step 7: QA — expiration 13 mois**

Console sur n'importe quelle page après un accept :

```js
const c = JSON.parse(localStorage.getItem('cookie-consent'))
c.timestamp = Date.now() - 14 * 30 * 24 * 60 * 60 * 1000 // 14 mois en arrière
localStorage.setItem('cookie-consent', JSON.stringify(c))
location.reload()
```

**Attendu :** la bannière réapparaît (consentement expiré, on repart en pending).

- [ ] **Step 8: QA — page /confidentialite**

Visite `/confidentialite` :

1. Pas d'occurrence des phrases « Aucun cookie », « strictement anonyme », « exemption CNIL ».
2. Section « Mesure d'audience » mentionne PostHog ET Google Analytics 4 avec Consent Mode v2.
3. Date « mai 2026 ».

- [ ] **Step 9: QA — mobile (DevTools viewport)**

DevTools > Toggle device toolbar > iPhone 12 Pro :

1. La bannière passe en layout vertical, 3 boutons empilés pleine largeur.
2. Le bouton « Accepter » (primaire) est en haut visuellement (ordre column-reverse).
3. La modal s'adapte au viewport, scrollable si besoin.
4. Le footer s'empile en colonne unique.

- [ ] **Step 10: QA — adblocker (uBlock Origin ou équivalent)**

Active uBlock Origin sur localhost et reload :

1. Le script `gtag/js` est bloqué (Network montre `(blocked:other)`).
2. **Aucune erreur dans la console JavaScript** (le code gère l'absence de `window.gtag` chargé).
3. La bannière s'affiche toujours et reste fonctionnelle.
4. PostHog passe aussi (via `/_ph/...` reverse-proxy, normalement non bloqué).

- [ ] **Step 11: QA — Do Not Track**

Active DNT dans le navigateur (Firefox : Préférences > Confidentialité > « Toujours » pour Do Not Track) et reload incognito :

1. La bannière s'affiche **quand même** (le choix utilisateur prime sur DNT).
2. Si accept → GA4 charge cookies (Google ne respecte pas DNT).
3. Si accept → PostHog **ne capture rien** (config `respect_dnt: true`).

- [ ] **Step 12: Vérifier qu'aucun emoji n'a été ajouté**

```bash
cd /Users/mathieu/Documents/survivor
grep -rnP "[\x{1F300}-\x{1F9FF}]|[\x{2600}-\x{27BF}]" app/components/CookieBanner.vue app/pages/cookies.vue app/pages/confidentialite.vue app/components/AppFooter.vue 2>/dev/null
```

**Attendu :** aucune sortie (pas d'emoji).

- [ ] **Step 13: Build production check**

```bash
npm run build
```

**Attendu :** build OK, prerender de `/`, `/cookies`, `/confidentialite`, etc., sans erreur. CSS scopé propre.

- [ ] **Step 14: Commit final (si corrections QA appliquées) ou tag de fin**

Si des bugs ont été corrigés pendant le QA :

```bash
git add <fichiers corrigés>
git commit -m "fix(consent): QA pass corrections"
```

Sinon, pas de commit nécessaire.

- [ ] **Step 15: Mémo post-déploiement**

Une fois mergé et déployé en production :
- Ajouter `NUXT_PUBLIC_GTAG_ID=G-KTGGEP09JY` dans les variables d'environnement Vercel.
- Vérifier dans GA4 (DebugView) que les hits arrivent correctement après acceptation.
- Vérifier dans PostHog que les session recordings apparaissent pour les opt-in.
- Linker GA4 à Google Search Console (admin GA4, hors-code).
- Mettre à jour la mémoire `reference_posthog.md` pour acter le passage en mode consent-gated.

---

## Récap des commits attendus

1. `feat(analytics): add GA4 env var, expose runtimeConfig, allow googletagmanager in CSP`
2. `feat(consent): add useConsent composable with 13mo TTL`
3. `feat(analytics): add gtag.js plugin with Consent Mode v2`
4. `feat(analytics): gate PostHog full mode behind consent, keep cookieless fallback`
5. `feat(consent): add CookieBanner component (slide-up + modal)`
6. `feat(consent): mount CookieBanner globally via ClientOnly`
7. `feat(footer): add legal section with cookies management`
8. `feat(cookies): add /cookies page with detail and management button`
9. `docs(privacy): update /confidentialite for GA4 + PostHog consent-gated mode`
10. (optionnel) `fix(consent): QA pass corrections`
